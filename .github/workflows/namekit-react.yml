name: namekit-react

on:
  push:
    branches:
      - main
    paths:
      - "packages/namekit-react/**"
  pull_request:
    branches:
      - main
    # paths:
    #   - "packages/namekit-react/**"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "lts/*"
      - name: Install Dependencies
        run: pnpm install
      - name: Build namekit-react
        run: pnpm --filter ./packages/namekit-react build
      - name: Test namekit-react
        run: pnpm --filter ./packages/namekit-react test
