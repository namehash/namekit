[tool.poetry]
name = "nameguard"
version = "0.1.0"
description = ""
authors = []
readme = "README.md"
packages = [
    { include = "nameguard" }
]


[tool.poetry.dependencies]
python = ">=3.9,<3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
